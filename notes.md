## Interesting question to ponder upon

### 1. What if I design a neural network for a particular purpose / dataset, but then train it on a different yet similar dataset; would that work ?

### 2. Exploring the design of different types of layers in a neural network; a) sparse layers, b) convolution layers etc.
